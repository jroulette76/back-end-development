export function greet(name: string) {
    const message = `Hello, ${name}`;
    return message; // Place breakpoint here for debugging
}

export function add(a: number, b: number) {
    const sum = a + b;
    return sum;
}

greet("World"); // Place breakpoint here for debugging